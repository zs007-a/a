name: Fetch

on:
  workflow_dispatch:
  push:
    path:
     - "test.txt"

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: 迁出代码
        uses: actions/checkout@v4
      - name: curl
        run: curl --slient ip-api.com >> a.test
