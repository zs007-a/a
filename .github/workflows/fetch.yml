name: Fetch

on:
  workflow_dispatch:
  push:
    path:
     - "a.test"
permissions: write-all

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: 迁出代码
        uses: actions/checkout@v4
      - name: curl
        run: |
          curl --silent ip-api.com >> a.test
          pwd
          ls -al
      - name: 提交更改
        run: |
          git config --local user.email "shuizhang654@gmail.com"
          git config --local user.name "zs"
          git add *
          git push
      - name: 推送更改
        uses: ad-m/github-push-action@master
        with: 
         branch: main
