name: Fetch

on:
  workflow_dispatch:
  push:
    path:
     - "test.txt"

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: 迁出代码
        uses: actions/checkout@v4
      - name: curl
        run: curl --silent ip-api.com >> a.test
      - name: 提交更改
        run: |
          git config --local user.email "shuizhang654@gmail"
          git config --local user.name "zs"
          git add test.txt
          git commit -m "add a.test"
          git push origin main
